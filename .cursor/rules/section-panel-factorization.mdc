---
description: Factor tab/detail components by section panels as the default architecture
globs: frontend/src/views/**/*.tsx
alwaysApply: false
---

# Section Panel Factorization

Use `view-architecture-pattern.mdc` as the canonical decomposition rule.

This rule adds one hard trigger:

- If a tab/component grows to multiple independent section panels, extract those panels into focused components before adding more feature logic.
