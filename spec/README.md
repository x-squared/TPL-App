# Specification Structure

The `spec/` area is split into two streams:

- `testing/` for executable test specifications (`spec-case`, `partner-case`)
  - `testing/server/`
  - `testing/client-server/`
- `requirements/` for business/domain requirement specifications (`.md`)

Requirement documents stay prominent in `requirements/` root.
